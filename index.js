//let metas = ["jow","yoha"]

//console.log(metas[1] + " e " + metas[0]);

let meta = {
    value:'Ler um livro por dia',
    checked:false,
    isCheked:(info) => {
        console.log(info)
    }
}

meta.log(meta.value);

const criarMetas = () => {}

//function criarMetas() {}